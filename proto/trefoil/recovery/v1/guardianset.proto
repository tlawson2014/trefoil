syntax = "proto3";
package trefoil.recovery.v1;

option go_package = "trefoil/x/recovery/types";

// Guardianset defines the Guardianset message.
message Guardianset {
  string owner = 1;
  uint64 threshold = 2;
  repeated string guardians = 3;
}
