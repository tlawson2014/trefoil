syntax = "proto3";
package trefoil.recovery.v1;

option go_package = "trefoil/x/recovery/types";

// Safedestinations defines the Safedestinations message.
message Safedestinations {
  string owner = 1;
  repeated string addresses = 2;
}
